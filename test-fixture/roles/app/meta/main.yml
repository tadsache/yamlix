---
galaxy_info:
  role_name: app
  author: yamlix
  description: Demo application role
  license: MIT
  min_ansible_version: "2.14"

dependencies:
  # NAV: role dependency -> roles/common. Runs BEFORE roles/app/tasks/main.yml.
  - role: common
    vars:
      common_reason: dependency-of-app
