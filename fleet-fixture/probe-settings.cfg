---
# NAV: F25 — named by `site-probe-cfg.yml`'s `vars_files:` and therefore loaded
# as YAML, whatever the extension says. Ansible does not care what a vars file
# is called; nothing but a play saying so makes this one.
probe_cfg_setting: from-cfg-file
